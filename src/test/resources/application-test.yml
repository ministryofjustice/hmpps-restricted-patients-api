server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

prison.api.endpoint.url: http://localhost:8999
prisoner.search.api.endpoint.url: http://localhost:8100
oauth.endpoint.url: http://localhost:8090/auth

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/restricted-patients-api-db
    username: test
    password: test
    continueOnError: true

domain-events-sqs:
  provider: localstack
  endpoint.url: http://localhost:4566
  queue.name: rp_api_queue
  dlq.name: rp_api_dlq